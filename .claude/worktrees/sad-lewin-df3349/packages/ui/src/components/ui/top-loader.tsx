'use client'

import * as React from 'react'

export const TopLoader = (props: any) => {
  return null
}

export const useTopLoader = () => {
  // Dummy implementation for now
  return {
    start: () => {},
    finish: () => {},
  }
}
