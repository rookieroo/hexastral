# svc-geocode — Cloudflare Worker Secrets
# This service has no runtime secrets — geocoding uses Nominatim (open, no API key required).
# The only external configuration is NOMINATIM_EMAIL (a [var] in wrangler.jsonc, not a secret).
# No .env.production.secrets file is required for deployment.
