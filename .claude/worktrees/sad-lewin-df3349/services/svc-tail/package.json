{
  "name": "@zhop/svc-tail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets:sync": "bun ../../scripts/sync-secrets.js --app=svc-tail --env=production",
    "secrets:sync:dry": "bun ../../scripts/sync-secrets.js --app=svc-tail --env=production --dry-run",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250606.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.90.0"
  }
}
