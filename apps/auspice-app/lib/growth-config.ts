/**
 * Yuun growth identity. Free 黄历 is anonymous-first; Pro is a separate
 * `auspice_pro` subscription (login-at-subscribe). Storage prefix is frozen —
 * changing it orphans existing anonymous install ids.
 */
export const PORTFOLIO_STORAGE_PREFIX = 'pf_cycle'
export const PORTFOLIO_TARGET_APP = 'auspice'
