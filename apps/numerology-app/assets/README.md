# numerology-app assets

Placeholder icon/splash slot. Designer brief:

- `icon.png` — 1024×1024, must read at App Store size. Suggested motif:
  a stylized numeric grid (1–9) over an inky background; HexAstral marque
  in one corner.
- `splash.png` — 1242×2436 (or scale-equivalent), centered glyph on
  `#09090B` background (matches `app.json` splash backgroundColor).

Both files are required by Expo prebuild. Until they exist, copy
`apps/coin-cast-app/assets/icon.png` and `splash.png` as temporary stand-ins.
