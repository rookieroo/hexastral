// next-intl middleware rewrites bare `/` to `app/[locale]/page.tsx` (en with
// `localePrefix: as-needed`). This file is a Next.js root-route placeholder only.
import LandingPage from './[locale]/page'

export default LandingPage
