/**
 * Personal 八字 natal compute — re-exported from the shared @zhop/scenario-yuan
 * package (Phase 0 of the Yuel/Yuun split: the personal-reading compute is now
 * shared so Yuun's personal deep read reuses the exact same engine). Kept as a
 * thin shim so every `@/lib/solo/natal` consumer is untouched.
 */
export * from '@zhop/scenario-yuan/natal'
