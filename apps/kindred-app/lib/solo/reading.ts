/**
 * Personal reading analysis (五行 count, 大运 chain, dayun read) — re-exported from
 * the shared @zhop/scenario-yuan package (Phase 0 of the Yuel/Yuun split). Thin shim
 * so every `@/lib/solo/reading` consumer is untouched.
 */
export * from '@zhop/scenario-yuan/reading'
