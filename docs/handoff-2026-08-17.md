# Handoff 2026-08-17 — hexastral 上架冲刺状态

> 更新自 2026-08-14 handoff。覆盖 39 个新提交（08-14 → 08-17）与无 IAP 入口收紧工作。
> 新 session 请先读本文件 + [docs/apps/yuun/launch.md](apps/yuun/launch.md) + [docs/publish/README.md](../publish/README.md)。

## 0. 里程碑变化（08-14 之后）

1. **ASC 提交进入实操**：`eas.json` 已填真实 `ascAppId 6796927374`；iOS scheme 固定 `Yuun`；
   pbxproj objectVersion 70→77（修 EAS 云端 pod install）；全套四语×双尺寸截图脚本
   （`docs/publish/screenshots/`，8 镜位范本 + watchOS 表盘脚本）。
2. **销售范围排除中国大陆**（外国主体 + 无 ICP，无法国区上架）。
3. **无 IAP 入口收紧落地**（本 session 前期工作，已合入）：Pro/Yuel 入口全部
   `isIapEnabled()` / `FLAGSHIP_LIVE.yuan` 门控——免费构建零死胡同付费墙；ASO
   Widget/Watch 声明删除 + parity 脚本 `_widgetEvidence` 强拦截；登录对 RevenueCat
   硬依赖修复（`lib/account.ts` aliasRevenueCatIfEnabled）。
4. **时间轴全部免费**：所有大运/流年/流月 + 对你而言判语（零 LLM）人人可用；
   Pro 只剩解读深度（per-node LLM deep-read、MonthlyDepth、印证）。页面无付费墙行。
5. **Make-if**：Pro-only 端到端；无 IAP 时 Library 入口隐藏，deep link 落中性提示。
6. **亲友生日上限 3→30**（机制保留，Pro 钩子移到对你而言/时间轴）；ASO 四语已删
   "最多 3 位"表述。
7. **黄历模式（classical voice）**：`voiceMode: contemporary | classical`，开关 =
   首页撕页黄历 + 黄历大组件（iOS WidgetKit + Android Glance 双端，
   `data.classical=true` 时 systemLarge/4×4 渲染历书纸页）。语域切换不新增预测宣称
   （ADR-0003 姿态不变）。
8. **推送质量系统**：24 个确定性变体（宜忌 3/4/5 词窗 + 冲煞 + 值神/彭祖/宿）+
   `last_body_key` 防重复（migration 0039）+ 送达/打开指标闭环（migration 0040，
   `auspice_push_sends/opens`，device 匿名哈希，非鉴权非结算）+ App 打开上报。
9. **satellite-runtime**：no-IAP 构建跳过 RevenueCat configure（防测试 key 在
   release 模式杀 App）。

## 1. 小组件统一规格（本 session 关键架构件）

- **SSOT**：`apps/auspice-app/lib/widget-spec.json`（盒子/pt/圆角/padding/字号/行数，
  含 `family.large.almanac.*` 黄历大组件）。
- RN 预览直接 import；Swift/Kotlin 由 `bun run widget-spec:gen` 生成
  `WidgetSpec.swift` / `WidgetSpec.kt`；`widget-spec:check` 已入 preflight（漂移即失败）。
- Android 预览：直角→16dp 圆角（`systemAppWidgetBackgroundRadius=16dp` 已声明，
  MIUI 忽略仍直角）；中尺寸 4×2 宽盒；Glance 与 WidgetKit 字号已统一
  （中 27 / 大 34 / padding 16 均匀）。
- 预览页 /display：竖向排列、按屏宽缩放；锁屏 mock 无底无框（iPhone 专属）；
  Watch 热区独立；长说明文字已删。

## 2. 推送内容结构（本轮答疑结论，供后续决策）

- 8:00 daily：无生辰 = 干支日+宜忌；**有生辰（登录与否）= +「你吉/平/凶」+ corpus
  hookLine**；Pro = +理由。钩子门槛 = 生辰，不是登录（MVP 走量决策，
  2026-08-17 已改：`renderAuspicePush` `showPersonal = personalization != null`）。
- **黄历模式设备**刻意压掉判语与 hookLine（classicalZh 分支，保持纯行话）。
- **推送尾部 disclaimer 已删（2026-08-17 决策）**：`renderAuspicePush`（morning/
  evening）+ 客户端 `lib/push.ts` fallback 全部移除——黄金位置不放无关信息。
  App 内 paywall/结果/聊天的免责声明与商店文案原样保留（审核真正的检查面）。
- **打分层（多信号打分 + band 化 + 语料扩充）= 独立 plan**：
  [docs/apps/yuun/push-corpus-taxonomy-plan.md](apps/yuun/push-corpus-taxonomy-plan.md)，
  MVP 不上，待推送指标 A/B 数据出来后实施。

## 3. 已知漂移/债

- `docs/apps/yuun/launch.md` 亲友行仍写 "Free cap 3"（实际 30，见 §0.6）。
- `packages/widget-kit-android/android/.gradle|build/reports` 未 ignore（git status
  仅此两个 untracked）。
- preflight 的根 `bun test` 在低 fd 配额沙箱会 EMFILE（本机/正常环境无碍）。
- **Secrets 安全清理（2026-08-17）**：`apps/hexastral-api/.env.production.secrets`
  本地明文文件已删（从未进 git，`.gitignore:42` 覆盖；Cloudflare 已持有全部值）。
  **人工项**：dashboard 吊销该文件里用过的 Cloudflare API Token（明文躺在盘上 6 周）。
  今后 secrets 只走 `bunx wrangler secret put`（AGENTS 规则）。`sync-secrets.js`
  脚本暂留（无输入文件则惰性），可择机删除。
- **Google 登录 401 待人工对齐**：`GOOGLE_OAUTH_AUDIENCES`（Cloudflare 变量）必须
  包含 eas.json 里 iOS/Android 两个 client id；建议顺手写进 `wrangler.jsonc` vars
  根治漂移。

## 4. 剩余关键人工项（未变）

- Worker secrets / `ALLOW_DEV_PRO=0` / `CYCLE_CALENDAR_SECRET`；API 部署（需批准）。
- 法律页 `/privacy/yuun` 四语 200。
- Widget/Watch 真机证据矩阵（拿到后 `_widgetEvidence=true` 恢复 ASO 声明）。
- `bun run preflight` 绿后 EAS production（`EXPO_PUBLIC_IAP_ENABLED=false`）→ TestFlight → 提审。
