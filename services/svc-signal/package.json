{
  "name": "@zhop/svc-signal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "secrets:sync": "bun ../../scripts/sync-secrets.js --app=svc-signal --env=production",
    "secrets:sync:dry": "bun ../../scripts/sync-secrets.js --app=svc-signal --env=production --dry-run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@zhop/astro-core": "workspace:*",
    "@zhop/astro-i18n": "workspace:*",
    "@zhop/logger": "workspace:*",
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250924.0",
    "@zhop/typescript-config": "workspace:*",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
