{
  "name": "@zhop/svc-notify",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "secrets:sync": "bun ../../scripts/sync-secrets.js --app=svc-notify --env=production",
    "secrets:sync:dry": "bun ../../scripts/sync-secrets.js --app=svc-notify --env=production --dry-run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@zhop/logger": "workspace:*",
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@zhop/typescript-config": "workspace:*",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
