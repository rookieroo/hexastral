/**
 * @deprecated — moved to `@zhop/ai-vision/cache`.
 *
 * Kept as a re-export shim for existing imports. New code imports directly
 * from `@zhop/ai-vision`.
 */

export {
  type CacheEntry,
  cacheKey,
  canonicalize,
  readCache,
  writeCache,
} from '@zhop/ai-vision'
