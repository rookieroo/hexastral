{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "hexastral-svc-mailer",
  "main": "src/index.ts",
  "workers_dev": false,
  "compatibility_date": "2025-09-03",
  "compatibility_flags": ["nodejs_compat"],

  // No public routes – accessed only via Service Binding
  // Callers: hexastral-api, notionflare (binding SVC_MAILER → hexastral-svc-mailer)
  // Caller adds: { "binding": "SVC_MAILER", "service": "hexastral-svc-mailer" }

  "vars": {
    "AWS_REGION": "us-east-1",
    "AWS_SES_FROM": "noreply@hexastral.com",
    "ALLOWED_FROM_EMAILS": "noreply@hexastral.com,noreply@notionflare.com",
    "ALLOWED_CALLERS": "hexastral-api,notionflare-newsletter"
  },

  // Secrets (via `wrangler secret put`):
  //   AWS_ACCESS_KEY_ID      - IAM用户，只需 ses:SendEmail 权限
  //   AWS_SECRET_ACCESS_KEY  - IAM密钥

  // Tail worker
  "tail_consumers": [{ "service": "svc-tail" }]
}
